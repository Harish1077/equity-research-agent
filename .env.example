# ── LLM credentials — provide at least ONE of these ────────────
# Preferred: Claude 3.5 Sonnet
ANTHROPIC_API_KEY=

# Fallback: used automatically if ANTHROPIC_API_KEY is not set
OPENAI_API_KEY=

# ── News context (optional) ─────────────────────────────────────
# Powers the Bull/Bear agents' access to recent news. If omitted,
# the graph still runs — Bull and Bear will reason from fundamentals
# and general business-model knowledge only.
TAVILY_API_KEY=
